import { Activity } from 'lucide-react';
import PlaceholderModule from '../common/PlaceholderModule';

export default function OperationsModule() {
  return (
    <PlaceholderModule
      icon={Activity}
      title="Operations"
      description="Model Drift and Monitoring. This area is a placeholder in the current demo — the Dashboard Configuration Studio (under Configuration) is the only fully implemented page."
    />
  );
}
