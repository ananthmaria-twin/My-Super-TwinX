import { LayoutGrid } from 'lucide-react';
import PlaceholderModule from '../common/PlaceholderModule';

export default function CatalogModule() {
  return (
    <PlaceholderModule
      icon={LayoutGrid}
      title="Catalog"
      description="Use case and feature catalog. This area is a placeholder in the current demo — the Dashboard Configuration Studio (under Configuration) is the only fully implemented page."
    />
  );
}
