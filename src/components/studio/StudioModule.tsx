import { Sparkles } from 'lucide-react';
import PlaceholderModule from '../common/PlaceholderModule';

export default function StudioModule() {
  return (
    <PlaceholderModule
      icon={Sparkles}
      title="Studio"
      description="Custom node editor and design-time contracts. This area is a placeholder in the current demo — the Dashboard Configuration Studio (under Configuration) is the only fully implemented page."
    />
  );
}
