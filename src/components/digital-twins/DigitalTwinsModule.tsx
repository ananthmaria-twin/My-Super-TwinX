import { Network } from 'lucide-react';
import PlaceholderModule from '../common/PlaceholderModule';

export default function DigitalTwinsModule() {
  return (
    <PlaceholderModule
      icon={Network}
      title="Digital Twins"
      description="Entity ontology, lineage, storage sync, and SQL query preview. This area is a placeholder in the current demo — the Dashboard Configuration Studio (under Configuration) is the only fully implemented page."
    />
  );
}
