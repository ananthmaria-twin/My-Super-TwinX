import { Database } from 'lucide-react';
import PlaceholderModule from '../common/PlaceholderModule';

export default function DataSourcesModule() {
  return (
    <PlaceholderModule
      icon={Database}
      title="Data Sources"
      description="Source system connectors and sync health. This area is a placeholder in the current demo — the Dashboard Configuration Studio (under Configuration) is the only fully implemented page."
    />
  );
}
