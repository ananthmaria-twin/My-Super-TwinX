import { Zap } from 'lucide-react';
import PlaceholderModule from '../common/PlaceholderModule';

export default function LiveFeaturesModule() {
  return (
    <PlaceholderModule
      icon={Zap}
      title="Live Features"
      description="Sub-millisecond feature serving and telemetry streaming. This area is a placeholder in the current demo — the Dashboard Configuration Studio (under Configuration) is the only fully implemented page."
    />
  );
}
